---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: repositories.image.theriseunion.io
spec:
  group: image.theriseunion.io
  names:
    categories:
    - registry
    kind: Repository
    listKind: RepositoryList
    plural: repositories
    shortNames:
    - repo
    singular: repository
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.repository
      name: Repository
      type: string
    - jsonPath: .status.domain
      name: Domain
      type: string
    - jsonPath: .status.artifactCount
      name: Artifacts
      type: integer
    - jsonPath: .status.latestTag
      name: LatestTag
      type: string
    - jsonPath: .status.syncStatus
      name: SyncStatus
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Repository is the Schema for the repositories API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RepositorySpec defines the desired state of Repository
            properties:
              dockerConfigSecretRef:
                description: DockerConfigSecretRef references a docker-registry type
                  Secret containing credentials
                properties:
                  name:
                    description: Name is the name of the Secret
                    type: string
                required:
                - name
                type: object
              repository:
                description: Repository is the Harbor repository path in format "project/repository"
                type: string
            required:
            - dockerConfigSecretRef
            - repository
            type: object
          status:
            description: RepositoryStatus defines the observed state of Repository
            properties:
              artifactCount:
                description: ArtifactCount is the number of artifacts in this repository
                format: int64
                type: integer
              domain:
                description: Domain is the Harbor domain parsed from the Secret
                type: string
              lastSynced:
                description: LastSynced is the last time the status was synchronized
                format: date-time
                type: string
              lastUpdated:
                description: LastUpdated is the last update time of the repository
                  in Harbor
                format: date-time
                type: string
              latestTag:
                description: LatestTag is the most recent tag in this repository
                type: string
              pullCount:
                description: PullCount is the total pull count of this repository
                format: int64
                type: integer
              syncMessage:
                description: SyncMessage contains error message if sync failed
                type: string
              syncStatus:
                description: SyncStatus indicates the synchronization status (Success/Failed/Syncing)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
