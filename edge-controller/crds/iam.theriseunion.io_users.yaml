---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: users.iam.theriseunion.io
spec:
  group: iam.theriseunion.io
  names:
    categories:
    - iam
    kind: User
    listKind: UserList
    plural: users
    singular: user
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.email
      name: Email
      type: string
    - jsonPath: .spec.displayName
      name: DisplayName
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: User is the Schema for the users API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserSpec defines the desired state of User
            properties:
              description:
                description: Description provides additional information about the
                  user
                type: string
              displayName:
                description: DisplayName is the user's display name
                type: string
              email:
                description: Email is the user's email address
                type: string
              encryptedPassword:
                description: |-
                  EncryptedPassword is the bcrypt hash of the user's password
                  This field is write-only and will not be returned in API responses
                type: string
              groups:
                description: Groups represents the groups this user belongs to
                items:
                  type: string
                type: array
              lang:
                description: Lang represents the user's preferred language
                type: string
            type: object
          status:
            description: UserStatus defines the observed state of User
            properties:
              lastLoginTime:
                description: LastLoginTime records when the user last successfully
                  logged in
                format: date-time
                type: string
              reason:
                description: Reason provides additional information about the current
                  state
                type: string
              state:
                description: State indicates the current state of the user
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
