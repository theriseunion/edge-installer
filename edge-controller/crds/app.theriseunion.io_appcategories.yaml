---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: appcategories.app.theriseunion.io
spec:
  group: app.theriseunion.io
  names:
    categories:
    - app
    kind: AppCategory
    listKind: AppCategoryList
    plural: appcategories
    shortNames:
    - appcat
    singular: appcategory
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName.zh
      name: DisplayName
      type: string
    - jsonPath: .spec.icon
      name: Icon
      type: string
    - jsonPath: .spec.order
      name: Order
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AppCategory is the Schema for the appcategories API
          Used for categorizing applications in the app store
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AppCategorySpec defines the desired state of AppCategory
            properties:
              description:
                additionalProperties:
                  type: string
                description: Description represents description for this category,
                  this field supports localization
                type: object
              displayName:
                additionalProperties:
                  type: string
                description: DisplayName represents the name displays at console,
                  this field supports localization
                type: object
              icon:
                description: Icon represents the icon for this category (emoji or
                  icon name)
                type: string
              order:
                description: Order represents the display order of this category (lower
                  number = higher priority)
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
