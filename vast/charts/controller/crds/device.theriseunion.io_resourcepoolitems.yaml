---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: resourcepoolitems.device.theriseunion.io
spec:
  group: device.theriseunion.io
  names:
    kind: ResourcePoolItem
    listKind: ResourcePoolItemList
    plural: resourcepoolitems
    singular: resourcepoolitem
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: The scope of the resource pool item
      jsonPath: .spec.scope
      name: Scope
      type: string
    - description: The name of the resource pool
      jsonPath: .spec.resourcePoolName
      name: ResourcePoolName
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ResourcePoolItem is the Schema for the resourcepoolitems API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResourcePoolItemSpec defines the desired state of ResourcePoolItem
            properties:
              resourcePoolName:
                description: ResourcePoolName specifies the name of the resource pool
                  (e.g., "resourcepool-sample").
                type: string
              scope:
                description: Strategy specifies the scope of the resource pool (e.g.,
                  "global", "node", "device").
                enum:
                - global
                - node
                - device
                type: string
            type: object
          status:
            description: ResourcePoolItemStatus defines the observed state of ResourcePoolItem.
            properties:
              items:
                description: Items stores the status of the devices in the resource
                  pool item.
                items:
                  description: ItemStatus defines the observed state of a device in
                    the resource pool item.
                  properties:
                    allocated:
                      description: Allocated represents the current resource allocated
                        of the GPU card.
                      properties:
                        core:
                          format: int32
                          type: integer
                        count:
                          format: int32
                          type: integer
                        memory:
                          format: int32
                          type: integer
                        splitCount:
                          format: int32
                          type: integer
                      required:
                      - core
                      - count
                      - memory
                      - splitCount
                      type: object
                    capacity:
                      description: Capacity represents the total resource capacity
                        of the GPU card.
                      properties:
                        core:
                          format: int32
                          type: integer
                        count:
                          format: int32
                          type: integer
                        memory:
                          format: int32
                          type: integer
                        splitCount:
                          format: int32
                          type: integer
                      required:
                      - core
                      - count
                      - memory
                      - splitCount
                      type: object
                    coreName:
                      description: CoreName specifies the core name of the device
                        (e.g., "nvidia.com/gpu-core").
                      type: string
                    deviceModel:
                      description: DeviceModelName specifies the device model name
                        (e.g., "nvidia-a100").
                      type: string
                    failedReason:
                      description: FailedReason represents the reason why the GPU
                        card is failed.
                      enum:
                      - NodeNotReady
                      - NodeUnScheduler
                      - DeviceOffline
                      - DevicePluginError
                      - Unknown
                      type: string
                    memoryName:
                      description: MemoryName specifies the memory name of the device
                        (e.g., "nvidia.com/gpu-memory").
                      type: string
                    name:
                      description: Name specifies the device name (e.g., "nvidia-a100-1").
                      type: string
                    nodeName:
                      description: NodeName identifies the node where the GPU card
                        is located (matches ResourceItem.NodeName).
                      type: string
                    resourceName:
                      description: ResourceName specifies the resource name of the
                        device (e.g., "nvidia.com/gpu").
                      type: string
                    status:
                      description: Status represents the status of the GPU card.
                      enum:
                      - Available
                      - Allocated
                      - Reserved
                      - Failed
                      - Unknown
                      type: string
                    uuid:
                      description: UUID identifies the GPU card (matches ResourceItem.UUID).
                      type: string
                  required:
                  - uuid
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
