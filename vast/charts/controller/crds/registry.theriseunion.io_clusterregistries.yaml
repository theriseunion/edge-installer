apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: clusterregistries.registry.theriseunion.io
spec:
  group: registry.theriseunion.io
  names:
    kind: ClusterRegistry
    listKind: ClusterRegistryList
    plural: clusterregistries
    singular: clusterregistry
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: The domain of the cluster registry
      jsonPath: .spec.domain
      name: Domain
      type: string
    - description: The provider of the cluster registry
      jsonPath: .spec.provider
      name: Provider
      type: string
    - description: The username for the cluster registry
      jsonPath: .spec.userName
      name: Username
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterRegistry is the Schema for the clusterregistries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              domain:
                type: string
              enabled:
                type: boolean
              password:
                type: string
              provider:
                type: string
              userName:
                type: string
            type: object
          status:
            description: ClusterRegistryStatus defines the observed state of ClusterRegistry
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
