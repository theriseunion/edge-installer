apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cert-manager.io/inject-ca-from: edge-system/edge-serving-cert
    controller-gen.kubebuilder.io/version: v0.16.1
  name: iamrolebindings.iam.theriseunion.io
spec:
  group: iam.theriseunion.io
  names:
    categories:
    - iam
    kind: IAMRoleBinding
    listKind: IAMRoleBindingList
    plural: iamrolebindings
    singular: iamrolebinding
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.roleRef.name
      name: Role
      type: string
    - jsonPath: .metadata.labels['iam\.theriseunion\.io/scope']
      name: Scope-Type
      type: string
    - jsonPath: .metadata.labels['iam\.theriseunion\.io/scope-value']
      name: Scope-Target
      type: string
    - jsonPath: .spec.subjects[*]
      name: Subjects
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          IAMRoleBinding is the Schema for the iamrolebindings API
          This is our internal role binding abstraction that gets converted to K8s RoleBinding/ClusterRoleBinding by controllers
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IAMRoleBindingSpec defines the desired state of IAMRoleBinding
            properties:
              roleRef:
                description: |-
                  RoleRef references an IAMRole in the current namespace or cluster scope
                  If the RoleRef cannot be resolved, the Authorizer must return an error
                properties:
                  apiGroup:
                    description: APIGroup is the group for the resource being referenced
                    type: string
                  kind:
                    description: Kind is the type of resource being referenced
                    type: string
                  name:
                    description: Name is the name of resource being referenced
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
                x-kubernetes-map-type: atomic
              subjects:
                description: |-
                  Subjects holds references to the objects the role applies to
                  For now, uses standard rbacv1.Subject for compatibility
                  Future: Will reference our IAM User/Group CRDs
                items:
                  description: |-
                    Subject contains a reference to the object or user identities a role binding applies to.  This can either hold a direct API object reference,
                    or a value for non-objects such as user and group names.
                  properties:
                    apiGroup:
                      description: |-
                        APIGroup holds the API group of the referenced subject.
                        Defaults to "" for ServiceAccount subjects.
                        Defaults to "rbac.authorization.k8s.io" for User and Group subjects.
                      type: string
                    kind:
                      description: |-
                        Kind of object being referenced. Values defined by this API group are "User", "Group", and "ServiceAccount".
                        If the Authorizer does not recognized the kind value, the Authorizer should report an error.
                      type: string
                    name:
                      description: Name of the object being referenced.
                      type: string
                    namespace:
                      description: |-
                        Namespace of the referenced object.  If the object kind is non-namespace, such as "User" or "Group", and this value is not empty
                        the Authorizer should report an error.
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
            required:
            - roleRef
            type: object
          status:
            description: IAMRoleBindingStatus defines the observed state of IAMRoleBinding
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
