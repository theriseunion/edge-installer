---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: scopepatterns.iam.theriseunion.io
spec:
  group: iam.theriseunion.io
  names:
    kind: ScopePattern
    listKind: ScopePatternList
    plural: scopepatterns
    singular: scopepattern
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.pattern
      name: Pattern
      type: string
    - jsonPath: .spec.scopeType
      name: ScopeType
      type: string
    - jsonPath: .status.registered
      name: Registered
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ScopePattern defines URL pattern matching rules for scope extraction
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScopePatternSpec defines the desired state of ScopePattern
            properties:
              description:
                description: Description of this pattern
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels for pattern categorization and selection
                type: object
              pattern:
                description: |-
                  URL pattern to match using web-standard path parameter syntax
                  Supports named parameters: {workspace}, {cluster}, {nodegroup}
                  Supports anonymous placeholders: {_} to skip unwanted path segments
                  Supports wildcards: * to match remaining path segments


                  Examples:
                    - "/api/edge/{nodegroup}/management/*" (extract nodegroup)
                    - "/apis/custom.io/{_}/workspaces/{workspace}/apps/*" (skip version, extract workspace)
                    - "/api/clusters/{_}/regions/{_}/nodegroups/{nodegroup}/*" (skip cluster and region)
                type: string
              scopeType:
                description: Type of scope to extract, must match a named parameter
                  in pattern (not {_})
                type: string
            required:
            - pattern
            - scopeType
            type: object
          status:
            description: ScopePatternStatus defines the observed state of ScopePattern
            properties:
              lastUsed:
                description: Last time this pattern was used
                format: date-time
                type: string
              matchCount:
                description: Number of times this pattern has been matched
                format: int64
                type: integer
              message:
                description: Human-readable message about the current state
                type: string
              registered:
                description: Whether the pattern is successfully registered
                type: boolean
            type: object
        type: object
        x-kubernetes-validations:
        - message: scopeType must match a named parameter in pattern
          rule: self.spec.pattern.contains('{' + self.spec.scopeType + '}')
    served: true
    storage: true
    subresources:
      status: {}
