apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cert-manager.io/inject-ca-from: edge-system/edge-serving-cert
    controller-gen.kubebuilder.io/version: v0.16.1
  name: reverseproxies.iam.theriseunion.io
spec:
  group: iam.theriseunion.io
  names:
    kind: ReverseProxy
    listKind: ReverseProxyList
    plural: reverseproxies
    singular: reverseproxy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.upstream.host
      name: Host
      type: string
    - jsonPath: .spec.upstream.port
      name: Port
      type: integer
    - jsonPath: .spec.matcher.path
      name: Path
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ReverseProxy general reverse proxy resource definition
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ReverseProxySpec defines the desired state of ReverseProxy
            properties:
              directives:
                description: Proxy directives and transformation rules
                properties:
                  authProxy:
                    description: Enable authentication proxy
                    type: boolean
                  headerDown:
                    description: Response header modifications
                    items:
                      type: string
                    type: array
                  headerUp:
                    description: Request header modifications
                    items:
                      type: string
                    type: array
                  method:
                    description: Override HTTP method
                    type: string
                  replace:
                    description: Content replacement rules
                    items:
                      description: ReverseProxyReplaceRule defines content replacement
                        rules
                      properties:
                        from:
                          description: Source pattern (regex)
                          type: string
                        to:
                          description: Target replacement
                          type: string
                      required:
                      - from
                      - to
                      type: object
                    type: array
                  rewrite:
                    description: Path rewrite rules
                    items:
                      description: ReverseProxyRewriteRule defines path rewriting
                        rules
                      properties:
                        from:
                          description: Source pattern (regex)
                          type: string
                        to:
                          description: Target replacement
                          type: string
                      required:
                      - from
                      - to
                      type: object
                    type: array
                  stripPathPrefix:
                    description: Remove path prefix
                    type: string
                  stripPathSuffix:
                    description: Remove path suffix
                    type: string
                  wrapTransport:
                    description: Wrap transport for additional processing
                    type: boolean
                type: object
              matcher:
                description: Request matching rules
                properties:
                  method:
                    description: HTTP method to match (* for all methods)
                    type: string
                  path:
                    description: Path pattern to match (supports wildcards)
                    type: string
                required:
                - path
                type: object
              upstream:
                description: Upstream service configuration
                properties:
                  caBundle:
                    description: CA certificate bundle (Base64 encoded)
                    type: string
                  host:
                    description: Upstream host
                    type: string
                  insecureSkipVerify:
                    description: Skip TLS certificate verification
                    type: boolean
                  port:
                    description: Upstream port
                    format: int32
                    type: integer
                  scheme:
                    description: Protocol scheme (http/https)
                    type: string
                required:
                - host
                - port
                - scheme
                type: object
            required:
            - matcher
            - upstream
            type: object
          status:
            description: ReverseProxyStatus defines the observed state of ReverseProxy
            properties:
              lastUpdateTime:
                description: Last time the status was updated
                format: date-time
                type: string
              message:
                description: Human-readable message describing the current state
                type: string
              state:
                description: Current state of the proxy
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
