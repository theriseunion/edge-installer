---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: iamroles.iam.theriseunion.io
spec:
  group: iam.theriseunion.io
  names:
    categories:
    - iam
    kind: IAMRole
    listKind: IAMRoleList
    plural: iamroles
    singular: iamrole
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.labels['iam\.theriseunion\.io/scope']
      name: Scope
      type: string
    - jsonPath: .metadata.labels['iam\.theriseunion\.io/scope-value']
      name: Scope-Value
      type: string
    - jsonPath: .spec.aggregationRoleTemplates.templateNames[*]
      name: Templates
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          IAMRole is the Schema for the iamroles API
          This is our internal role abstraction that gets converted to K8s Role/ClusterRole by controllers
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IAMRoleSpec defines the desired state of IAMRole
            properties:
              aggregationRoleTemplates:
                description: |-
                  AggregationRoleTemplates describes how to locate RoleTemplates to aggregate into this IAMRole
                  Follows the standard pattern for template-based role composition
                properties:
                  roleSelector:
                    description: RoleSelector select rules from RoleTemplate's rules
                      by labels
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  templateNames:
                    description: TemplateNames select rules from RoleTemplate's rules
                      by RoleTemplate name
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              rules:
                description: |-
                  Rules holds all the PolicyRules for this IAMRole
                  Uses standard K8s rbacv1.PolicyRule for full ecosystem compatibility
                  These rules are treated as Allow rules with default priority
                items:
                  description: |-
                    PolicyRule holds information that describes a policy rule, but does not contain information
                    about who the rule applies to or which namespace the rule applies to.
                  properties:
                    apiGroups:
                      description: |-
                        APIGroups is the name of the APIGroup that contains the resources.  If multiple API groups are specified, any action requested against one of
                        the enumerated resources in any API group will be allowed. "" represents the core API group and "*" represents all API groups.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    nonResourceURLs:
                      description: |-
                        NonResourceURLs is a set of partial urls that a user should have access to.  *s are allowed, but only as the full, final step in the path
                        Since non-resource URLs are not namespaced, this field is only applicable for ClusterRoles referenced from a ClusterRoleBinding.
                        Rules can either apply to API resources (such as "pods" or "secrets") or non-resource URL paths (such as "/api"),  but not both.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    resourceNames:
                      description: ResourceNames is an optional white list of names
                        that the rule applies to.  An empty set means that everything
                        is allowed.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    resources:
                      description: Resources is a list of resources this rule applies
                        to. '*' represents all resources.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    verbs:
                      description: Verbs is a list of Verbs that apply to ALL the
                        ResourceKinds contained in this rule. '*' represents all verbs.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                  required:
                  - verbs
                  type: object
                type: array
              uiPermissions:
                description: |-
                  UIPermissions holds UI permission identifiers for frontend integration
                  Examples: "workload/deployment/create", "monitoring/alerts/view"
                items:
                  type: string
                type: array
            type: object
          status:
            description: IAMRoleStatus defines the observed state of IAMRole
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
