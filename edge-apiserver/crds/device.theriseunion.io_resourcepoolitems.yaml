---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: resourcepoolitems.device.theriseunion.io
spec:
  group: device.theriseunion.io
  names:
    kind: ResourcePoolItem
    listKind: ResourcePoolItemList
    plural: resourcepoolitems
    singular: resourcepoolitem
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: The scope of the resource pool item
      jsonPath: .spec.scope
      name: Scope
      type: string
    - description: The name of the resource pool
      jsonPath: .spec.resourcePoolName
      name: ResourcePoolName
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ResourcePoolItem is the Schema for the resourcepoolitems API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResourcePoolItemSpec defines the desired state of ResourcePoolItem
            properties:
              resourcePoolName:
                description: ResourcePoolName specifies the name of the resource pool
                  (e.g., "resourcepool-sample").
                type: string
              scope:
                description: Strategy specifies the scope of the resource pool (e.g.,
                  "global", "node", "device").
                enum:
                - global
                - node
                - device
                type: string
            type: object
          status:
            description: ResourcePoolItemStatus defines the observed state of ResourcePoolItem.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
