apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edge-console.fullname" . }}-test-connection"
  labels:
    {{- include "edge-console.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edge-console.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
